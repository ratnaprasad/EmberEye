# Simulator V2 Testing Guide (Retired)

The v2 simulator has been removed from `simulators/`.

Use one of the current simulators instead:

- `simulators/tcp_sensor_simulator_v3.py` (repo root) for protocol-compliant streaming
- `simulators/pfds/pfds_simulator.py` for PFDS log replay

See:
- `simulators/README.md`
- `simulators/pfds/README.md`
